# Classification

This directory contains the NumPy implementations for the binary and
multiclass neural-network classification project. The numbered modules build
from a single neuron through a persistent deep neural network with selectable
hidden-layer activations.
